# Tasks - Day 11

Use these commands as a practical checklist for the day. Replace placeholders such as `<...>` before running them.

## Commands

| Command | What it does |
|---|---|
| `cd 2026/day-11` | Changes into the target directory before running the exercise commands. |
| `touch devops-file.txt team-notes.txt project-config.yaml` | Creates one or more empty files if they do not already exist. |
| `sudo useradd -m tokyo` | Creates a Linux user, usually with a home directory when -m is used. |
| `sudo useradd -m berlin` | Creates a Linux user, usually with a home directory when -m is used. |
| `sudo useradd -m professor` | Creates a Linux user, usually with a home directory when -m is used. |
| `sudo groupadd heist-team` | Creates a Linux group. |
| `ls -l devops-file.txt` | Lists files, directories or metadata such as permissions and ownership. |
| `sudo chown tokyo devops-file.txt` | Changes file or directory owner and optionally group. |
| `sudo chown berlin devops-file.txt` | Changes file or directory owner and optionally group. |
| `sudo chgrp heist-team team-notes.txt` | Changes group ownership for the target path. |
| `sudo chown professor:heist-team project-config.yaml` | Changes file or directory owner and optionally group. |
| `mkdir -p app-logs heist-project/vault heist-project/plans bank-heist` | Creates the requested directory path. |
| `touch heist-project/vault/gold.txt heist-project/plans/strategy.conf` | Creates one or more empty files if they do not already exist. |
| `sudo groupadd planners` | Creates a Linux group. |
| `sudo chown -R professor:planners heist-project/` | Changes file or directory owner and optionally group. |
| `ls -lR heist-project/` | Lists files, directories or metadata such as permissions and ownership. |
