# Day 11 Challenge

## Files and Directories Created

- `devops-file.txt`
- `team-notes.txt`
- `project-config.yaml`
- `app-logs/`
- `heist-project/`
- `bank-heist/`

## Ownership Changes

Record before/after output from `ls -l` for each change.

## Commands Used

Use the commands from `tasks.md`.

## What I Learned

- Ownership is split into user and group.
- `chgrp` changes only the group.
- Recursive ownership changes should be verified with `ls -lR`.

