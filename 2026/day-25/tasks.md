# Tasks - Day 25

Use these commands as a practical checklist for the day. Replace placeholders such as `<...>` before running them.

## Commands

| Command | What it does |
|---|---|
| `git commit --allow-empty -m "Commit A"` | Creates a commit from staged changes with the provided message. |
| `git commit --allow-empty -m "Commit B"` | Creates a commit from staged changes with the provided message. |
| `git commit --allow-empty -m "Commit C"` | Creates a commit from staged changes with the provided message. |
| `git reset --soft HEAD~1` | Moves HEAD and optionally updates the index and working tree. |
| `git status` | Shows changed, staged and untracked files in the repository. |
| `git commit -m "Commit C again"` | Creates a commit from staged changes with the provided message. |
| `git reset --mixed HEAD~1` | Moves HEAD and optionally updates the index and working tree. |
| `git status` | Shows changed, staged and untracked files in the repository. |
| `git commit -am "Commit C again"` | Creates a commit from staged changes with the provided message. |
| `git reset --hard HEAD~1` | Moves HEAD and optionally updates the index and working tree. |
| `git reflog` | Shows recent movements of HEAD for recovery and auditing. |
| `git revert <commit-hash>` | Creates a new commit that reverses an earlier commit. |
