# Tasks - Day 18

Use these commands as a practical checklist for the day. Replace placeholders such as `<...>` before running them.

## Commands

| Command | What it does |
|---|---|
| `cd 2026/day-18` | Changes into the target directory before running the exercise commands. |
| `chmod +x *.sh` | Changes file or directory permissions. |
| `./functions.sh` | Runs the functions demo with greet and add functions. |
| `./disk_check.sh` | Runs disk and memory check functions. |
| `./strict_demo.sh` | Runs the strict mode demo for set -euo pipefail behaviour. |
| `./local_demo.sh` | Runs the local-variable demo and shows variable scope behaviour. |
| `./system_info.sh` | Runs the system information reporter built from functions. |
