# Tasks - Day 33

Use these commands as a practical checklist for the day. Replace placeholders such as `<...>` before running them.

## Commands

| Command | What it does |
|---|---|
| `docker compose version` | Checks that Docker Compose is installed and prints its version. |
| `cd 2026/day-33/compose-basics` | Changes into the target directory before running the exercise commands. |
| `docker compose up -d` | Creates and starts the services defined in the Compose file. |
| `docker compose ps` | Lists Compose services and their current state. |
| `docker compose logs` | Shows logs for all Compose services or a specific service. |
| `curl -I http://localhost:8080` | Makes an HTTP request or checks HTTP headers for the target URL. |
| `docker compose down` | Stops and removes Compose containers and the default network. |
| `cd ../wordpress-mysql` | Changes into the target directory before running the exercise commands. |
| `cp env.sample .env` | Copies files or directories from source to destination. |
| `docker compose up -d` | Creates and starts the services defined in the Compose file. |
| `docker compose ps` | Lists Compose services and their current state. |
| `docker compose logs mysql` | Shows logs for all Compose services or a specific service. |
| `docker compose down` | Stops and removes Compose containers and the default network. |
| `docker compose up -d` | Creates and starts the services defined in the Compose file. |
| `docker compose down` | Stops and removes Compose containers and the default network. |
