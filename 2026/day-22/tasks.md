# Tasks - Day 22

Use these commands as a practical checklist for the day. Replace placeholders such as `<...>` before running them.

## Commands

| Command | What it does |
|---|---|
| `cd 2026/day-22` | Changes into the target directory before running the exercise commands. |
| `mkdir devops-git-practice` | Creates the requested directory path. |
| `cd devops-git-practice` | Changes into the target directory before running the exercise commands. |
| `git init` | Initializes a new Git repository in the current directory. |
| `git config user.name "Your Name"` | Reads or sets Git configuration values such as user name and email. |
| `git config user.email "you@example.com"` | Reads or sets Git configuration values such as user name and email. |
| `git status` | Shows changed, staged and untracked files in the repository. |
| `ls -la .git` | Lists files, directories or metadata such as permissions and ownership. |
| `touch git-commands.md` | Creates one or more empty files if they do not already exist. |
| `git add git-commands.md` | Stages the listed files for the next commit. |
| `git commit -m "Add git commands reference"` | Creates a commit from staged changes with the provided message. |
| `git log --oneline` | Displays commit history, often in a compact or graph view. |
