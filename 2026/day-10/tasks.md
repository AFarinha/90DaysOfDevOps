# Tasks - Day 10

Use these commands as a practical checklist for the day. Replace placeholders such as `<...>` before running them.

## Commands

| Command | What it does |
|---|---|
| `cd 2026/day-10` | Changes into the target directory before running the exercise commands. |
| `touch devops.txt` | Creates one or more empty files if they do not already exist. |
| `echo "Linux file permissions practice" > notes.txt` | Prints text, often redirected into a file or appended to one. |
| `printf '#!/bin/bash\necho "Hello DevOps"\n' > script.sh` | Prints formatted text, used here to create script content. |
| `ls -l devops.txt notes.txt script.sh` | Lists files, directories or metadata such as permissions and ownership. |
| `cat notes.txt` | Prints file contents to the terminal. |
| `head -n 5 /etc/passwd` | Shows the first lines of a file. |
| `tail -n 5 /etc/passwd` | Shows the last lines of a file, commonly used for logs. |
| `chmod +x script.sh` | Changes file or directory permissions. |
| `./script.sh` | Runs a local script or executable from the current directory. |
| `chmod a-w devops.txt` | Changes file or directory permissions. |
| `chmod 640 notes.txt` | Changes file or directory permissions. |
| `mkdir -p project` | Creates the requested directory path. |
| `chmod 755 project` | Changes file or directory permissions. |
| `ls -l devops.txt notes.txt script.sh` | Lists files, directories or metadata such as permissions and ownership. |
| `ls -ld project` | Lists files, directories or metadata such as permissions and ownership. |
