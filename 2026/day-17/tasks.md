# Tasks - Day 17

Use these commands as a practical checklist for the day. Replace placeholders such as `<...>` before running them.

## Commands

| Command | What it does |
|---|---|
| `cd 2026/day-17` | Changes into the target directory before running the exercise commands. |
| `chmod +x *.sh` | Changes file or directory permissions. |
| `./for_loop.sh` | Runs the for-loop example that prints a list of fruits. |
| `./count.sh` | Runs the for-loop counter that prints numbers from 1 to 10. |
| `./countdown.sh` | Runs the while-loop countdown script using user input. |
| `./greet.sh Andre` | Runs the argument-based greeting script with Andre as the first argument. |
| `./args_demo.sh one two three` | Runs the argument demo and prints script name, argument count and all arguments. |
| `sudo ./install_packages.sh` | Runs the package installer script as root so it can install missing packages. |
| `./safe_script.sh` | Runs the error-handling demo that creates and enters /tmp/devops-test. |
