# Tasks - Day 12

Use these commands as a practical checklist for the day. Replace placeholders such as `<...>` before running them.

## Commands

| Command | What it does |
|---|---|
| `cd 2026/day-12` | Changes into the target directory before running the exercise commands. |
| `ps aux \| head` | Lists running processes with the requested format. |
| `systemctl status ssh` | Shows status, recent logs and health information for a systemd service. |
| `journalctl -u ssh -n 20` | Reads logs from journald, optionally filtered by service and line count. |
| `mkdir -p revision-demo` | Creates the requested directory path. |
| `echo "revision" > revision-demo/file.txt` | Prints text, often redirected into a file or appended to one. |
| `chmod 640 revision-demo/file.txt` | Changes file or directory permissions. |
| `ls -l revision-demo/file.txt` | Lists files, directories or metadata such as permissions and ownership. |
| `touch day-12-revision.md` | Creates one or more empty files if they do not already exist. |
