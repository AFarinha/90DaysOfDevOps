# Day 13 LVM

## Commands Used

Use `tasks.md` and replace `/dev/loop0` with the actual loop or block device from your system.

## What I Learned

- LVM separates physical disks from logical volumes.
- Volume groups provide a pool of storage.
- Logical volumes can be extended when applications need more space.

