# Tasks - Day 20

Use these commands as a practical checklist for the day. Replace placeholders such as `<...>` before running them.

## Commands

| Command | What it does |
|---|---|
| `cd 2026/day-20` | Changes into the target directory before running the exercise commands. |
| `chmod +x sample_logs_generator.sh log_analyzer.sh` | Changes file or directory permissions. |
| `./sample_logs_generator.sh sample_log.log 200` | Generates a sample log file with 200 lines for analyzer testing. |
| `./log_analyzer.sh sample_log.log` | Analyzes the sample log and generates a dated summary report. |
| `ls -l log_report_*.txt` | Lists files, directories or metadata such as permissions and ownership. |
| `cat log_report_$(date +%Y-%m-%d).txt` | Prints file contents to the terminal. |
