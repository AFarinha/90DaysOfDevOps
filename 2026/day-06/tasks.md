# Tasks - Day 06

Use these commands as a practical checklist for the day. Replace placeholders such as `<...>` before running them.

## Commands

| Command | What it does |
|---|---|
| `cd 2026/day-06` | Changes into the target directory before running the exercise commands. |
| `touch notes.txt` | Creates one or more empty files if they do not already exist. |
| `echo "Line 1: DevOps needs clear notes." > notes.txt` | Prints text, often redirected into a file or appended to one. |
| `echo "Line 2: Logs and configs are text files." >> notes.txt` | Prints text, often redirected into a file or appended to one. |
| `echo "Line 3: Practice makes commands faster." \| tee -a notes.txt` | Prints text, often redirected into a file or appended to one. |
| `cat notes.txt` | Prints file contents to the terminal. |
| `head -n 2 notes.txt` | Shows the first lines of a file. |
| `tail -n 2 notes.txt` | Shows the last lines of a file, commonly used for logs. |
| `touch file-io-practice.md` | Creates one or more empty files if they do not already exist. |
