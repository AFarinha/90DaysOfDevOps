# Day 26 Notes

## Authentication

`gh` supports browser-based login, device flow and token-based authentication depending on environment.

## Automation Ideas

- Create issues from scripts.
- Query PR status in release checks.
- Read workflow run status in CI/CD dashboards.
- Use `--json` for machine-readable output.

