services:
  web:
    image: nginx:latest
    ports:
      - "${WEB_PORT}:80"

  db:
    image: mysql:8.0
    ports:
      - "${MYSQL_PORT}:3306"
    environment:
      MYSQL_ROOT_PASSWORD: "${MYSQL_ROOT_PASSWORD}"