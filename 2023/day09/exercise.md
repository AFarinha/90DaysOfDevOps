# Configs
git config --global user.name "Name"
git config --global user.email "your-email@example.com"

# Confirm
git config --global --list


cat > Day-02.txt << EOF
Day 02 - Git and GitHub

Today I practiced creating files, adding changes to Git, committing them, and pushing the commits to GitHub.

Git is important for DevOps because it helps track changes, collaborate with teams, and manage code, scripts, pipelines, and configuration files.
EOF