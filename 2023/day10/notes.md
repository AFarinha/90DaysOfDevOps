Merge combines the history of two branches and usually creates a merge commit. It preserves the real history of the branches.

Rebase moves the commits from one branch and places them on top of another branch. It creates a cleaner and more linear history.

Merge is safer for shared branches because it does not rewrite history. Rebase is useful to keep a branch updated with the latest changes from master before merging.